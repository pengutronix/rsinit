[package]
name = "rsinit"
version = "0.1.0"
edition = "2021"
license = "GPL-2.0-only"

[dependencies]
nix = { version = "0.29", features = ["ioctl", "fs", "mount", "process", "term"] }
getrandom = { version = "0.2.15" }
